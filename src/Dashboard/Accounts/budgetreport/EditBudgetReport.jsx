/**
 * LEGACY ENTRY — old EditBudgetReport (AG Grid by report _id) is disabled.
 * Active implementation: ./excel/BudgetReportExcelEdit.jsx
 *
 * Route param is now the confirmed event id (`:id` / `:eventId`).
 */
export { default } from "./excel/BudgetReportExcelEdit";
